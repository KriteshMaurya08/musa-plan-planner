import {createFileRoute} from "@tanstack/react-router";import {useState} from "react";import {AppShell,SectionHeading} from "@/components/musa/app-shell";import {PlanCard,PlanComparison} from "@/components/musa/plan-components";import {plans} from "@/data/mock-scholarships";import {Button} from "@/components/ui/button";export const Route=createFileRoute("/plans")({head:()=>({meta:[{title:"Compatible Scholarship Plans — MUSA CodeX"},{name:"description",content:"Compare conflict-free scholarship combinations by value and requirements."},{property:"og:title",content:"Compatible Scholarship Plans — MUSA CodeX"},{property:"og:description",content:"Compare conflict-free scholarship combinations by value and requirements."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Page});function Page(){const [sort,setSort]=useState("benefit");const ordered=[...plans].sort((a,b)=>sort==="documents"?a.documents-b.documents:sort==="confidence"?b.confidence-a.confidence:b.benefit-a.benefit);return <AppShell title="Compatible Scholarship Plans" eyebrow="Combination analysis / 05 valid"><div className="mb-7 max-w-3xl text-sm leading-6 text-muted-foreground">MUSA generated these plans from scholarships you are eligible for and removed combinations that violate detected compatibility rules.</div><SectionHeading code="PLAN SET / SORTABLE" title="Generated Plans" action={<div className="flex gap-1">{[["benefit","Benefit"],["documents","Documents"],["confidence","Confidence"]].map(([v,l])=><Button key={v} size="sm" variant={sort===v?"default":"outline"} onClick={()=>setSort(v)}>{l}</Button>)}</div>}/><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{ordered.map(p=><PlanCard key={p.id} plan={p}/>)}</div><div className="mt-10"><SectionHeading code="SIDE-BY-SIDE" title="Plan Comparison"/><PlanComparison/></div></AppShell>}
