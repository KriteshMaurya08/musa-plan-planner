import { createFileRoute } from "@tanstack/react-router";import { useState } from "react";import { AppShell,SectionHeading } from "@/components/musa/app-shell";import { ScholarshipTable } from "@/components/musa/scholarship-table";import { ScholarshipDetailPanel } from "@/components/musa/detail-panel";import type { Scholarship } from "@/data/mock-scholarships";
export const Route=createFileRoute("/scholarships")({head:()=>({meta:[{title:"Eligible Scholarships — MUSA CodeX"},{name:"description",content:"Review analyzed scholarships and eligibility evidence."},{property:"og:title",content:"Eligible Scholarships — MUSA CodeX"},{property:"og:description",content:"Review analyzed scholarships and eligibility evidence."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Page});function Page(){const [selected,setSelected]=useState<Scholarship|null>(null);return <AppShell title="Scholarship Landscape"><SectionHeading code="ELIGIBILITY / 08 RESULTS" title="Eligible Scholarships"/><div className={selected?"grid gap-5 xl:grid-cols-[1fr_360px]":""}><ScholarshipTable onSelect={setSelected}/>{selected&&<ScholarshipDetailPanel scholarship={selected} onClose={()=>setSelected(null)}/>}</div></AppShell>}
