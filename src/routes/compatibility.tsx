import {createFileRoute} from "@tanstack/react-router";import {useState} from "react";import {AppShell,SectionHeading} from "@/components/musa/app-shell";import {CompatibilityGraph} from "@/components/musa/compatibility-graph";import {ScholarshipDetailPanel} from "@/components/musa/detail-panel";import type {Scholarship} from "@/data/mock-scholarships";
export const Route=createFileRoute("/compatibility")({head:()=>({meta:[{title:"Compatibility Network — MUSA CodeX"},{name:"description",content:"Explore compatible and conflicting scholarship relationships."},{property:"og:title",content:"Compatibility Network — MUSA CodeX"},{property:"og:description",content:"Explore compatible and conflicting scholarship relationships."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary"}]}),component:Page});function Page(){const [selected,setSelected]=useState<Scholarship|null>(null);return <AppShell title="Compatibility Network" eyebrow="Relationship analysis / live"><SectionHeading code="NETWORK / 08 NODES / 12 RULES" title="Scholarship Compatibility Network"/><div className={`grid gap-5 ${selected?"xl:grid-cols-[1fr_360px]":""}`}><CompatibilityGraph onSelect={setSelected}/>{selected&&<ScholarshipDetailPanel scholarship={selected} onClose={()=>setSelected(null)}/>}</div></AppShell>}
